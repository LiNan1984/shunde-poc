# Local secrets — never commit API keys / provider configs
.env
.env.*
!.env.example
*.pem
providers.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# OS / editor
.DS_Store
.idea/
.vscode/

# Scratch / local only
.scratch/
*.log
kb_store/
**/kb_store/

# Temp CSV staging dir used by scripts/append_proof_blocks.py
.cache/
**/*.pyc

# MinerU is large (models + venv); keep local only.
# Official QwenPaw is a git submodule (see .gitmodules) — do not vendor a fork.
MinerU/
