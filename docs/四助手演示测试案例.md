# 四助手演示测试案例（每场景 5 条）

每条案例的预期结果均取自 `poc/fixtures` 黄金回归集与夹具生成脚本的真实数据，可直接用于现场演示与回归验收。对应关系：案例 → 夹具 / 黄金值来源在「验证」列注明。

---

## ① Excel 文件问答助手（excel-qa-bank / excel_guard MCP）

| # | 测试问题 / 操作 | 预期结果 | 验证 |
|---|----------------|---------|------|
| E1 | 「顺德地区的销售额合计是多少？」（`销售明细.xlsx`） | `219950.0` | `poc/fixtures/qa_fixtures/baseline_answers.json → q01_shunde_total` |
| E2 | 「顺德2024年销售额是多少？」（`分区域汇总.xlsx`，两级合并表头） | `1250000.5`，且能正确读合并表头 | `baseline_answers.json → q06_sd_2024_sales` |
| E3 | 「全部商品的收入合计是多少？」（`公式列.xlsx`，公式列未缓存计算值） | `73814.0`，不把公式串当数据 | `baseline_answers.json → q08_revenue_total` |
| E4 | 「零售部一共有多少人？」（`网点名单.csv`，GBK 编码） | `12`，正确识别 GBK 而非乱码 | `baseline_answers.json → q10_retail_headcount` |
| E5 | 写回：「给销售明细增加利润列并另存」（`edit_workbook`） | 生成的文件与 `golden_利润列.xlsx` 一致（写前预检 + 写后 OpenXML 校验） | `poc/fixtures/qa_fixtures/golden_outputs/` 比对 |

## ② 多模态知识库问答助手（kb-qa-bank / kb_qa MCP）

| # | 测试问题 / 操作 | 预期结果 | 验证 |
|---|----------------|---------|------|
| K1 | 「对公信贷逾期90天的认定标准适用于哪些产品？」（`信贷政策.pdf`） | 答「所有对公贷款产品」，引用第 1 页 | `poc/fixtures/kb_golden_qa.json → credit-policy/p1` |
| K2 | 「资本充足率不得低于多少，由谁按季审议？」 | 关键词「资本充足率」「风险管理委员会」，第 2 页 | `kb_golden_qa.json → credit-policy/p2` |
| K3 | 「产品定价表里经营贷款的利率是多少？」（`产品定价.pdf` 表格） | `4.20`（表格 CSV 行命中） | `kb_golden_qa.json → pricing/table` |
| K4 | 「BranchC 网点的贷款余额是多少亿元？」（`网点余额图.pdf` 嵌入图表） | `3.69`，仅视觉模型 `analyze_page` 读图可答 | `kb_golden_qa.json → branch-balance/image`（离线检索必失配，是模态差距样本） |
| K5 | 「哪份文档介绍了各网点贷款余额？」（多文件路由） | 命中 `网点余额图.pdf` 文件级元数据 | `kb_golden_qa.json → branch-balance/routing` |

## ③ 运营助手（ops-assistant / ops_data MCP + ops-telemetry Hook 插件）

| # | 测试问题 / 操作 | 预期结果 | 验证 |
|---|----------------|---------|------|
| O1 | 「现在有哪些埋点文件？」（`list_telemetry_files`） | 列出 `POC_WORKSPACE/telemetry/*.jsonl`；沙箱外路径拒绝 | 工具返回清单，目录与 `POC_WORKSPACE` 一致 |
| O2 | 「汇总一下调用量，按工具分布」（`summarize_calls`） | 返回类别/状态/工具维度的调用次数统计 | 对任一 JSONL 行数与汇总总数核对 |
| O3 | 「最近 10 条失败事件」（`recent_events` + category 过滤） | 只返回最近且匹配类别的条目，`limit` 生效 | 返回条数 ≤ 10 且字段完整 |
| O4 | 安装 `poc/plugins/ops-telemetry` 插件后跑一轮会话 | Hook 自动写入：调用量、Token、工具成败、耗时、会话身份五类字段 | 新 JSONL 记录含五类字段；Console「已安装插件」可见 |
| O5 | 「这周调用量最多的工具是哪个？」 | 基于埋点数据给出工具名 + 次数，引用 JSONL 证据 | 与 `summarize_calls` 结果一致 |

## ④ 报告可视化助手（report-visualizer / report_visualizer MCP）

数据源：`poc/fixtures/report_demo/quarterly_business.json`（顺德分行 2025Q3 各支行零售业务）。

| # | 测试问题 / 操作 | 预期结果 | 验证 |
|---|----------------|---------|------|
| R1 | 「按支行 × 产品对金额做交叉表」（`pivot_table`） | 大良/容桂等支行 × 储蓄/个贷/理财的金额汇总矩阵 | 与源 JSON 逐项求和一致 |
| R2 | 「画各支行储蓄存款金额柱状图」（`render_bar`） | 生成 PNG，数值与源数据一致 | `poc/fixtures/report_demo/out/charts/` 已有基线图可对比 |
| R3 | 「画产品维度的多系列折线/饼图」（`render_line` / `render_pie`） | 饼图按标签汇总后比例正确 | 图内数值与 JSON 汇总一致 |
| R4 | 「生成一份含交叉表和所有图表的 DOCX 季度报告」（`render_docx_report`） | DOCX 含标题、章节、交叉表、嵌入图表 | `poc/fixtures/report_demo/out/report.docx` 为基线 |
| R5 | 「按支行 × 产品画金额热力图」（`render_heatmap`） | 二维矩阵渲染 PNG，行列维度与源数据一致；散点图可按 region 列着色（`render_scatter`） | 图内数值与 JSON 汇总一致 |

---

**回归口径**：Excel ① 用 `pytest poc/tests`（`qa_set.py` 黄金问答 + 写回校验器）；知识库 ② 离线检索用 `scripts/kb_eval.py`，视觉题需 `--live`；运营 ③、报告 ④ 以工具返回 + 基线产物比对为准。
